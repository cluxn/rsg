{
  "name": "rsg-monorepo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\" \"npm run dev --prefix admin\"",
    "build": "npm run build --prefix frontend && npm run build --prefix backend && npm run build --prefix admin"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
