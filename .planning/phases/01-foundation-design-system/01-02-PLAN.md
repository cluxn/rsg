---
phase: 1
plan: "01-02"
title: "MySQL Schema + Backend API Foundation"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - backend/src/db/connection.ts
  - backend/src/db/migrate.ts
  - backend/src/db/seed.ts
  - backend/src/db/migrations/001_initial_schema.sql
  - backend/src/middleware/auth.ts
  - backend/src/middleware/validate.ts
  - backend/src/routes/auth.ts
  - backend/src/routes/settings.ts
  - backend/src/routes/revalidate.ts
  - backend/src/controllers/auth.controller.ts
  - backend/src/controllers/settings.controller.ts
  - backend/src/services/auth.service.ts
  - backend/src/services/settings.service.ts
  - backend/src/index.ts
  - backend/.env.example
autonomous: true
requirements:
  - INFRA-02
  - INFRA-03
  - INFRA-04
---

<objective>
Create the complete MySQL schema (all 9 tables) via a migration script, wire the backend to the Hostinger remote DB, implement admin auth (login endpoint with httpOnly JWT cookie), protect admin routes via `requireAuth` middleware, build the settings GET/PUT API, and add the ISR revalidation endpoint. Result: Admin can log in via API and receive a JWT cookie; unprotected admin routes return 401; settings can be read and updated; `/api/revalidate` can trigger Next.js ISR.
</objective>

<tasks>

<task id="2.1" type="execute">
<name>Create MySQL connection pool and DB helper</name>
<read_first>
- .env (actual DB credentials: DB_HOST, DB_USER, DB_PASSWORD, DB_NAME — read values, do not hardcode)
- .planning/research/STACK.md (mysql2 3.x, createPool, parameterized queries)
- .planning/phases/01-foundation-design-system/01-RESEARCH.md (connection pool config: connectionLimit 5)
- backend/src/index.ts (current entry — verify how to export app for testing)
</read_first>
<action>
Create `backend/src/db/connection.ts`:
```ts
import mysql from 'mysql2/promise';
import dotenv from 'dotenv';
dotenv.config();

export const pool = mysql.createPool({
  host:            process.env.DB_HOST,
  port:            Number(process.env.DB_PORT) || 3306,
  user:            process.env.DB_USER,
  password:        process.env.DB_PASSWORD,
  database:        process.env.DB_NAME,
  connectionLimit: 5,
  waitForConnections: true,
});

export async function query<T = unknown>(sql: string, params?: unknown[]): Promise<T[]> {
  const [rows] = await pool.execute(sql, params);
  return rows as T[];
}
```
Update `backend/src/index.ts` to import `pool` and call `pool.query('SELECT 1')` on startup to verify DB connection (log success/error without crashing the server).
</action>
<acceptance_criteria>
- `backend/src/db/connection.ts` exports `pool` (mysql2 Pool) and `query` helper
- `query` function uses `pool.execute` (parameterized, not string interpolation)
- On `npm run dev`, backend logs `DB connected` (or similar) without crashing
- `cd backend && npx tsc --noEmit` exits 0
</acceptance_criteria>
</task>

<task id="2.2" type="execute">
<name>Write migration script (001_initial_schema.sql) with all 9 tables</name>
<read_first>
- .planning/phases/01-foundation-design-system/01-RESEARCH.md (full SQL DDL for all 9 tables: admin_users, settings, products, pages, media, blog_posts, events, testimonials, leads)
- .planning/REQUIREMENTS.md (INFRA-02: must include products, pages, leads, media, blog posts, events, testimonials, settings, admin users)
</read_first>
<action>
Create `backend/src/db/migrations/001_initial_schema.sql` with CREATE TABLE IF NOT EXISTS statements for all 9 tables exactly as specified in RESEARCH.md:
- `admin_users` (id, email UNIQUE, password_hash, name, created_at)
- `settings` (key PRIMARY KEY, value TEXT, updated_at)
- `products` (id, slug UNIQUE, name, description, specs JSON, display_order, created_at, updated_at)
- `pages` (id, slug UNIQUE, meta_title, meta_description, updated_at)
- `media` (id, filename, original_name, alt_text NOT NULL, mime_type, size, url, uploaded_at)
- `blog_posts` (id, slug UNIQUE, title, body LONGTEXT, meta_title, meta_description, published BOOLEAN, published_at, created_at, updated_at)
- `events` (id, slug UNIQUE, title, body LONGTEXT, event_date DATE, published BOOLEAN, created_at, updated_at)
- `testimonials` (id, text NOT NULL, author_name NOT NULL, author_city, rating DECIMAL(2,1), source ENUM, active BOOLEAN, created_at)
- `leads` (id, name NOT NULL, phone, email, product_interest, message, source_page, webhook_sent BOOLEAN, created_at)

Create `backend/src/db/migrate.ts` that reads and executes the SQL file using `pool.query()` (supports multi-statement DDL).
</action>
<acceptance_criteria>
- `backend/src/db/migrations/001_initial_schema.sql` exists and contains exactly 9 `CREATE TABLE IF NOT EXISTS` blocks
- Running `npx ts-node src/db/migrate.ts` against the Hostinger DB succeeds without error
- After migration, `SHOW TABLES` in the DB returns: admin_users, settings, products, pages, media, blog_posts, events, testimonials, leads
</acceptance_criteria>
</task>

<task id="2.3" type="execute">
<name>Create seed script for admin user + Phase 1 settings</name>
<read_first>
- .planning/phases/01-foundation-design-system/01-CONTEXT.md (D-12: seed settings keys — whatsapp_number, business_email: shivam.gupta09@gmail.com, seo_scripts: empty; D-11: settings API only in Phase 1, no admin UI yet)
- .planning/SCOPE-DECISIONS.md (10 product slugs: colour-coated-roofing-sheet, ms-plate-channel-angle, ms-pipe, decking-sheet, purlins, polycarbonate-sheet, crimping-sheet, self-drilling-screws, turbo-air-ventilator, accessories)
- backend/src/db/connection.ts (use query helper)
</read_first>
<action>
Create `backend/src/db/seed.ts`:
1. Insert admin user: email from `process.env.ADMIN_EMAIL || 'admin@rsgprofilesheets.com'`, bcrypt-hash `process.env.ADMIN_PASSWORD || 'Admin@RSG2024'` with `saltRounds: 12`. Use `INSERT IGNORE` to avoid duplicate-key errors on re-seed.
2. Insert settings rows with `INSERT IGNORE INTO settings (key, value) VALUES ...` for: `('whatsapp_number', '')`, `('business_email', 'shivam.gupta09@gmail.com')`, `('seo_scripts', '')`
3. Insert 10 product rows with `INSERT IGNORE INTO products (slug, name, display_order)` — use the exact 10 slugs from SCOPE-DECISIONS.md.
Add `"seed": "ts-node src/db/seed.ts"` to backend/package.json scripts.
</action>
<acceptance_criteria>
- `npm run seed` in backend/ completes without error
- After seed: `SELECT COUNT(*) FROM admin_users` returns ≥ 1
- `SELECT key, value FROM settings` returns rows for `whatsapp_number`, `business_email`, `seo_scripts`
- `SELECT COUNT(*) FROM products` returns 10
- Re-running `npm run seed` does not throw duplicate-key errors (INSERT IGNORE)
</acceptance_criteria>
</task>

<task id="2.4" type="execute">
<name>Implement requireAuth middleware + auth routes (login/logout/me)</name>
<read_first>
- .planning/phases/01-foundation-design-system/01-RESEARCH.md (JWT pattern, requireAuth code, httpOnly cookie, withCredentials)
- backend/src/db/connection.ts (query helper)
- backend/src/index.ts (existing middleware setup — verify cookie-parser is imported)
</read_first>
<action>
1. Create `backend/src/middleware/auth.ts`:
   - `requireAuth(req, res, next)`: reads `req.cookies?.token`; if missing → 401 `{ error: 'Unauthorized' }`; `jwt.verify(token, process.env.JWT_SECRET!)` → if invalid → 401; else set `req.admin = payload` and call `next()`.
   - Extend Express `Request` type via `declare global { namespace Express { interface Request { admin?: { id: number; email: string } } } }`

2. Create `backend/src/services/auth.service.ts`:
   - `findAdminByEmail(email: string)`: `SELECT id, email, password_hash FROM admin_users WHERE email = ?`
   - `verifyPassword(plain: string, hash: string)`: `bcrypt.compare(plain, hash)`
   - `signToken(payload: { id: number; email: string })`: `jwt.sign(payload, process.env.JWT_SECRET!, { expiresIn: '7d' })`

3. Create `backend/src/controllers/auth.controller.ts`:
   - `login(req, res)`: validate body has `email` (string) + `password` (string) → call auth service → on match, `res.cookie('token', token, { httpOnly: true, sameSite: 'strict', maxAge: 7*24*3600*1000 })` → `res.json({ ok: true, admin: { id, email } })`; on mismatch → 401 `{ error: 'Invalid credentials' }`
   - `logout(req, res)`: `res.clearCookie('token')` → `res.json({ ok: true })`
   - `me(req, res)`: returns `req.admin`

4. Create `backend/src/routes/auth.ts` mounting `POST /login`, `POST /logout`, `GET /me` (me uses requireAuth).
5. Mount auth router in `backend/src/index.ts` at `/api/auth`.
</action>
<acceptance_criteria>
- `POST http://localhost:4000/api/auth/login` with body `{"email":"admin@rsgprofilesheets.com","password":"Admin@RSG2024"}` returns 200 + `Set-Cookie: token=<jwt>; HttpOnly`
- `POST /api/auth/login` with wrong password returns 401 `{"error":"Invalid credentials"}`
- `GET http://localhost:4000/api/auth/me` without cookie returns 401
- `GET /api/auth/me` with valid cookie returns `{"id":1,"email":"admin@rsgprofilesheets.com"}`
- `cd backend && npx tsc --noEmit` exits 0
</acceptance_criteria>
</task>

<task id="2.5" type="execute">
<name>Implement settings GET/PUT API</name>
<read_first>
- .planning/phases/01-foundation-design-system/01-CONTEXT.md (D-11: Phase 1 delivers GET/PUT API only, no admin UI; D-12: three keys in Phase 1)
- backend/src/middleware/auth.ts (requireAuth — use on PUT route)
- backend/src/db/connection.ts (query helper)
</read_first>
<action>
1. Create `backend/src/services/settings.service.ts`:
   - `getAllSettings()`: `SELECT key, value FROM settings` → return as `Record<string, string>`
   - `getSetting(key: string)`: `SELECT value FROM settings WHERE key = ?`
   - `upsertSetting(key: string, value: string)`: `INSERT INTO settings (key, value) VALUES (?, ?) ON DUPLICATE KEY UPDATE value = VALUES(value), updated_at = NOW()`

2. Create `backend/src/controllers/settings.controller.ts`:
   - `getSettings(req, res)`: calls `getAllSettings()` → `res.json(result)`
   - `updateSettings(req, res)`: for each key-value pair in `req.body`, validate key is a non-empty string, value is a string → call `upsertSetting` → `res.json({ ok: true })`

3. Create `backend/src/routes/settings.ts`:
   - `GET /` → `getSettings` (public — frontend needs this for WhatsApp number, SEO scripts)
   - `PUT /` → `requireAuth`, `updateSettings`

4. Mount settings router in `backend/src/index.ts` at `/api/settings`.
</action>
<acceptance_criteria>
- `GET http://localhost:4000/api/settings` returns JSON with keys `whatsapp_number`, `business_email`, `seo_scripts`
- `PUT /api/settings` without JWT cookie returns 401
- `PUT /api/settings` with valid cookie and body `{"whatsapp_number":"9999999999"}` returns `{"ok":true}` and subsequent GET returns updated value
- `cd backend && npx tsc --noEmit` exits 0
</acceptance_criteria>
</task>

<task id="2.6" type="execute">
<name>Implement ISR revalidation endpoint</name>
<read_first>
- .planning/phases/01-foundation-design-system/01-RESEARCH.md (revalidation pattern: POST /api/revalidate, REVALIDATE_SECRET, calls Next.js /api/revalidate)
- backend/src/middleware/auth.ts (requireAuth — this endpoint is called by backend internally, not by admin UI directly, but should require auth or secret)
- backend/.env.example (REVALIDATE_SECRET, FRONTEND_URL keys)
</read_first>
<action>
Create `backend/src/routes/revalidate.ts`:
- `POST /api/revalidate`: requires `requireAuth` OR matches `req.body.secret === process.env.REVALIDATE_SECRET`
- Accepts body: `{ path?: string, tag?: string }`
- Validates: at least one of `path` or `tag` must be provided
- Calls `fetch(`${process.env.FRONTEND_URL}/api/revalidate`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ secret: process.env.REVALIDATE_SECRET, path, tag }) })`
- Returns `{ revalidated: true, path, tag }` on success; proxies error on failure

Also create `frontend/app/api/revalidate/route.ts` (the Next.js handler that receives the revalidation call):
```ts
import { revalidatePath, revalidateTag } from 'next/cache';
export async function POST(req: Request) {
  const { secret, path, tag } = await req.json();
  if (secret !== process.env.REVALIDATE_SECRET) return Response.json({ error: 'Unauthorized' }, { status: 401 });
  if (path) revalidatePath(path);
  if (tag) revalidateTag(tag);
  return Response.json({ revalidated: true, timestamp: Date.now() });
}
```
</action>
<acceptance_criteria>
- `POST http://localhost:4000/api/revalidate` with valid auth cookie and body `{"path":"/"}` calls `http://localhost:3000/api/revalidate` and returns `{"revalidated":true,...}`
- `POST http://localhost:3000/api/revalidate` with wrong secret returns 401
- `cd frontend && npx tsc --noEmit` exits 0
- `cd backend && npx tsc --noEmit` exits 0
</acceptance_criteria>
</task>

</tasks>

<verification>
1. Run `npm run migrate` then `npm run seed` in backend/ — both exit 0
2. `SHOW TABLES` in DB returns all 9 table names
3. `SELECT COUNT(*) FROM products` = 10
4. `POST /api/auth/login` with seeded credentials → 200 + httpOnly cookie
5. `GET /api/auth/me` with cookie → 200 with admin object
6. `GET /api/settings` → returns all 3 seeded keys
7. `PUT /api/settings` without cookie → 401
8. `cd backend && npx tsc --noEmit` exits 0
9. `cd frontend && npx tsc --noEmit` exits 0 (after adding revalidate route)
</verification>

<must_haves>
  <truths>
    - All DB queries use parameterized statements (never string interpolation with user input)
    - Admin routes (PUT settings, POST revalidate) require `requireAuth` middleware or secret — 401 without valid JWT cookie
    - JWT stored in httpOnly, SameSite=strict cookie — never accessible to JavaScript
    - Settings table uses key-value pattern (Pattern 3) — adding new keys in Phase 2 requires no migration
    - Migration script is idempotent (CREATE TABLE IF NOT EXISTS) — safe to re-run
    - Seed script is idempotent (INSERT IGNORE) — safe to re-run
  </truths>
</must_haves>

<threat_model>
  <threats>
    <threat severity="high">SQL injection via user-controlled input — mitigated by parameterized queries (`pool.execute(sql, params)`) throughout; no string concatenation with user input</threat>
    <threat severity="high">JWT secret hardcoded or weak — mitigated by `process.env.JWT_SECRET` (required, no default), .env in .gitignore, and bcrypt saltRounds: 12</threat>
    <threat severity="medium">Admin password in seed script visible in plain text — mitigated by using env var `process.env.ADMIN_PASSWORD` with bcrypt hashing before insertion; document in README to change before production</threat>
    <threat severity="medium">CORS + credentials misconfigured allowing any origin — mitigated by explicit origin whitelist in Plan 01-01 and `sameSite: 'strict'` on JWT cookie</threat>
    <threat severity="low">Revalidation endpoint callable without auth — mitigated by `requireAuth` OR `REVALIDATE_SECRET` check (both must be set in production)</threat>
  </threats>
</threat_model>

<success_criteria>
- INFRA-02: All 9 tables exist in MySQL after migration
- INFRA-03 (partial): Admin login endpoint works; full INFRA-03 (logging in via admin UI) confirmed in Plan 01-04
- INFRA-04: Unprotected admin routes (PUT /settings without cookie) return 401
- Settings GET/PUT API works end-to-end
- ISR revalidation endpoint works end-to-end
</success_criteria>
