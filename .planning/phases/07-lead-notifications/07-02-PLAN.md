---
phase: 7
plan: "07-02"
title: "Wire Notification + Admin Settings Field"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - backend/src/services/leads.service.ts
  - admin/src/pages/Settings.tsx
autonomous: true
requirements:
  - NOTIF-01
---

<objective>
Wire the notification service into createLead (after the n8n webhook block, same fire-and-forget try/catch pattern) and add the notification_email field to the admin General Settings form, completing NOTIF-01 end-to-end.
</objective>

<tasks>

<task id="7.3" type="execute">
<name>Update leads.service.ts — call sendLeadNotificationEmail after n8n webhook block</name>
<read_first>
- backend/src/services/leads.service.ts (full file — the n8n webhook block lines 22-38 is the exact pattern to replicate for email; createLead function)
- backend/src/services/notification.service.ts (sendLeadNotificationEmail signature — written in Wave 1)
</read_first>
<action>
In `backend/src/services/leads.service.ts`:

1. Add import at top:
   `import { sendLeadNotificationEmail } from './notification.service';`

2. After the closing `}` of the n8n webhook try/catch block (currently ends around line 38), add the email notification block:

```
// Fire email notification — failure is silent (D-12: lead is already in DB)
try {
  const sent = await sendLeadNotificationEmail({
    id: leadId,
    name,
    phone,
    email,
    product_interest,
    message,
    source_page,
    created_at: new Date().toISOString(),
  });
  if (sent) {
    await query('UPDATE leads SET email_sent = TRUE WHERE id = ?', [leadId]);
  }
} catch {
  // Email failure intentionally swallowed (D-12)
}
```

3. Update the `LeadRow` interface: add `email_sent: boolean;` after `webhook_sent: boolean;`.

4. Update `getLeads` SELECT statement: add `email_sent` to the column list after `webhook_sent`.

5. Update `exportLeadsToCSV` SELECT statement: add `email_sent` to the column list after `webhook_sent`.

6. Update `exportLeadsToCSV` stringify columns: add `{ key: 'email_sent', header: 'Email Sent' }` after the `webhook_sent` column entry.
</action>
<acceptance_criteria>
- `backend/src/services/leads.service.ts` imports `sendLeadNotificationEmail` from `./notification.service`
- `createLead` calls `sendLeadNotificationEmail` after the n8n webhook try/catch block with all lead fields
- On `sendLeadNotificationEmail` returning `true`, `UPDATE leads SET email_sent = TRUE WHERE id = ?` is called
- The try/catch around the email block swallows all errors silently
- `LeadRow` interface includes `email_sent: boolean`
- `getLeads` SELECT includes `email_sent` column
- `exportLeadsToCSV` SELECT and stringify columns include `email_sent` / `'Email Sent'`
- `cd backend && npx tsc --noEmit` exits 0
</acceptance_criteria>
</task>

<task id="7.4" type="execute">
<name>Update admin/src/pages/Settings.tsx — add notification_email field</name>
<read_first>
- admin/src/pages/Settings.tsx (full file — GENERAL_FIELDS array and GeneralSettingsForm pattern to extend)
</read_first>
<action>
In `admin/src/pages/Settings.tsx`:

1. Add one entry to the `GENERAL_FIELDS` const array (append after `business_hours`):
```tsx
{ key: 'notification_email', label: 'Notification Email', hint: 'Lead alert emails are sent here. Leave blank to disable notifications.', type: 'email' },
```

The `as const` cast on `GENERAL_FIELDS` automatically extends state initialization and form rendering — no other changes needed to `GeneralSettingsForm`. The existing `useMutation` calling `PUT /settings` already sends all keys including the new one.
</action>
<acceptance_criteria>
- `GENERAL_FIELDS` in `admin/src/pages/Settings.tsx` includes entry with `key: 'notification_email'`, `type: 'email'`
- `label` is `'Notification Email'` and `hint` references lead alerts and blank-to-disable
- `cd admin && npx tsc --noEmit` exits 0
- Running admin dev server and navigating to `/settings` shows the "Notification Email" input field below "Business Hours"
- Saving with a valid email address calls `PUT /settings` with `notification_email` in the payload
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `cd backend && npx tsc --noEmit` exits 0
2. `cd admin && npx tsc --noEmit` exits 0
3. Submit a Get Quote form → lead appears in admin Leads list with `email_sent` column visible
4. With `SMTP_*` env vars set and `notification_email` configured in Settings, submitting a form triggers a plain-text email to the configured address
5. With `notification_email` blank in Settings, no email is sent and the lead is still saved (no error)
6. With `SMTP_HOST` missing from env, no email is sent and the lead is still saved (no error)
7. Leads CSV export includes "Email Sent" column
</verification>

<must_haves>
  <truths>
    - Email notification block in createLead follows same fire-and-forget pattern as n8n webhook: after lead INSERT, in separate try/catch, failure swallowed (D-10, D-12)
    - email_sent flag updated only on confirmed delivery (sendLeadNotificationEmail returns true) (D-14)
    - notification_email field is the ONLY change to admin Settings — no new page, no new route (D-04)
    - exportLeadsToCSV must include email_sent column (D-11 + existing webhook_sent pattern)
  </truths>
</must_haves>

<threat_model>
  <threats>
    <threat severity="low">Notification call delays lead INSERT response — mitigated: lead is already inserted before notification fires; notification is awaited but its failure is swallowed</threat>
  </threats>
</threat_model>

<success_criteria>
- NOTIF-01: Submitting a Get Quote form triggers SMTP email to admin-configured address; notification failure does not block lead save
</success_criteria>
